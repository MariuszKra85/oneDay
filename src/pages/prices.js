import React from 'react';
import Layout from '../components/layout';

const prices = () => {
    return (
        <Layout>
            <h2>Cennik:</h2>
            <p>(tabela dopiero po rejestracji firmy) </p>
        </Layout>
    );
}

export default prices;
